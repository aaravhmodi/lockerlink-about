import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "About LockerLink - Connect, Compete, and Grow in Volleyball",
  description: "Learn about LockerLink, the volleyball platform connecting athletes, coaches, scouts, mentors, and parents. Discover features, modes, and how to get started.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

